# Mobile Command Center Implementation Plan

This plan outlines the architecture and tasks for building the frontend of the CIRO system: **The Mobile Command Center** (Track A).

## Proposed Changes

### 1. Dependency Initialization
We will navigate to the `ciro-mobile` directory and install the required dependencies:
- **Firebase:** `firebase` (Client SDK)
- **Maps:** `react-native-maps`
- **Navigation:** `@react-navigation/native`, `@react-navigation/native-stack`, `react-native-screens`, `react-native-safe-area-context`

*Note: We will run this command via the terminal.*

### 2. [NEW] `d:\CIRO\ciro-mobile\firebaseConfig.js`
Create a secure Firebase configuration template.
- Initializes the Firebase app using the web/client config.
- Exports the `db` (Firestore instance) for use across the app.
- Leaves the `apiKey` and other config values empty for manual entry to keep secrets out of source control.

### 3. [MODIFY] `d:\CIRO\ciro-mobile\App.js`
Replace the default Expo `App.js` with the primary routing structure.
- Set up a `NavigationContainer` and a `Stack.Navigator`.
- Add the `MapDashboard` as the initial screen.

### 4. [NEW] `d:\CIRO\ciro-mobile\MapDashboard.js`
Scaffold the core mapping screen.
- Utilize `react-native-maps` to render a `MapView` centered on a specific coordinate (e.g., Karachi or Islamabad).
- Implement a `useEffect` hook to set up `onSnapshot` listeners for both the `ActiveCrises` and `Resources` collections.
- For this initial stage, the listeners will simply log the retrieved data to the console, ensuring the connection works before building out the custom markers.

## Open Questions
None at this stage. The tasks follow standard React Native/Expo architecture.

## Verification Plan
1. Ensure the Expo dependency installation completes successfully.
2. Verify that `firebaseConfig.js`, `App.js`, and `MapDashboard.js` are created correctly.
3. Stop and request your code review before we move on to rendering the UI markers.

If you approve of this plan, I will proceed with installing the packages and writing the files!
